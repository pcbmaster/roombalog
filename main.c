#include <studio.h>
#include <string.h>

int main(int argc, string* argv){
  string port;
  if (argc > 1){
    //Load config file
  }
  else {
    //Parse arguments
  }
  FILE *sstream;
  if((sstream = fopen(port, "rw")) == NULL){
    //do stuff
  }
}
